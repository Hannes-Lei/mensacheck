import React from "react";
import ReactDOM from "react-dom/client";
import MensaCheck from "./MensaCheck.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MensaCheck />
  </React.StrictMode>
);